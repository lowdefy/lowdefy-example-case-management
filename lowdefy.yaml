config:
  auth:
    createUsers: invite_only

menus:
  - id: default
    links:
      - id: new-query
        type: MenuLink
        pageId: new-query
        properties:
          icon: AlertOutlined
          title: New Query
      - id: queries
        type: MenuLink
        pageId: queries
        properties:
          icon: ProfileOutlined
          title: Queries
global:
  statusColors:
    New: '#52c41a'
    Client Contacted: '#13c2c2'
    'Investigation Started': '#1890ff'

pages:
  - _ref: new-query.yaml
  - _ref: queries.yaml
